# CR triage — PR #23 (`SCRUM-213-MultiSigSetupForContractAdmin` → `dev`)

_Generated 2026-05-24 · commit `d6b2b94` · CR comments triaged: 43 inline + 6 reviews (covering ~30 unique outside-diff threads) = **52 distinct findings**_

PR title: `SCRUM-213: Safe-owned Diamond deployment via MultiSend` (head ref carries the SCRUM-213 work but the PR diff against `dev` is a giant aggregate that also includes the SCRUM-222 audit work; that is what CR reviewed).

## Summary

| Verdict | Count | Notes |
|---|---|---|
| ACCEPT-FIX | 12 | **Code (5):** CR-3291973200 (Medium · harness solvency floor), CR-3291973202 (Medium · `setMaxFeeRate` lost `NoChangeRequired`), CR-3291973203 (Medium · `_handlePayoutTransfer` zero-fee bypass), CR-3291973204 (Medium · `getPositionInfo` direct-storage read), CR-3291973184 (Medium · A-2 dead-code remediation must be fail-closed). **Docs (7):** CR-3291768582, CR-3291768585, CR-3291768587, CR-3291760046, CR-3291973166, CR-3291973171, CR-3291973191, CR-3291973196, CR-3291973199, REV-B, REV-C — all Low, all foldable into CR-DOCS-* sweep tickets. |
| REJECT-TEACH | 18 | of which `learn-globally`: 4 (markdown MD040 nits, NatSpec custom-tags on view facets, `@custom:security` on Bitcoin-oracle adapter, `audit/` developer-path complaint). Includes CR-3291760037 `.gitignore` mis-read, CR-3291829768 tone_instructions length false-positive (CR rated **Critical**, actual length is 244 chars), CR-3291760043 `ERC1155ReceiverFacet` claimed non-existent (it exists), REV-A repo-rename suggestion, REV-D raw-Slither-artifact-needs-dispositions ask, REV-H NonceManagerFacet "legacy ABI" hallucination, REV-F SEC-006 wording (pinned-doc convention), CR-3291973189 (same pattern), CR-3291973181 (pinned-doc), the 7 MD040 nits, and CR-3291973169 (`.coderabbit.yml` already cites the requested IDs). |
| DEFER-TICKET | 10 | All Low. Bundled into 8 follow-up tickets — see "Follow-up rows" section below. The biggest bundles are CR-CI-HARDEN (6 sub-comments), CR-DOCS-MD040 (10+ sub-comments), CR-USAGE-MATRIX (4 sub-comments). Single Medium: CR-3291760047 (`hardhat.config.js` verify-failure ops risk → CR-HARDHAT-VERIFY). |
| DISCUSS | 6 | (1) REV-H / CR-3291973205 — `NonceManagerFacet` blanket-tag NatSpec policy; (2) CR-3291973206 — `SettlementAdminFacet` same; (3) CR-3291973201 — harness reentrancy-latch behavioural-proxy choice; (4) CR-3291973181 — SEC-005 ledger entry rewrite vs footnote (pinned-commit convention); (5) CR-3291973193 — `audit/manifest.md` "uncommitted" claim; (6) CR-3291973195 — `audit/phase2-review/REVIEW.md` "no code changed" wording. |

CodeRabbit raised one true Medium and several Low-grade real defects. The bulk of the noise is markdown MD040 fence-language nits across audit/docs, MD001 heading hierarchy, dev-machine paths in `audit/` artifacts, and a recurring "every facet view function needs `@custom:security`/`@custom:audit`/`@custom:reverts`" generic-style request that this repo does not enforce uniformly (only the settlement core does — `.coderabbit.yml` `path_instructions` deliberately concentrates the security tagging there).

## Recurring false-positive patterns (≥2 occurrences)

1. **MD040 — "add language tag to fenced block"** in `audit/**`, `docs/**`, `.claude/CLAUDE.md`, `.github/CODERABBIT.md` — 17 occurrences across CR-3291760041, CR-3291768584, CR-3291768588, CR-3291829785, CR-3291973176, CR-3291973178, CR-3291973186, plus 7 outside-diff repeats in the review-level rollups (docs/flows/*, CODERABBIT.md, etc.). These are real markdownlint-MD040 nits but **not** a project priority — the repo has no markdownlint gate. We accept the policy ("tag fences in audit/ + docs/") and propose a one-shot sweep ticket rather than per-comment patches. See `PR-23.teach.yml`.
2. **Generic NatSpec custom-tag request on view-only facets** (`MarketDataFacet`, `OracleAdapterFacet`, `AdminConfigFacet` view fns, `NonceManagerFacet`, `SettlementAdminFacet`) — 9 occurrences across CR-3291973205, CR-3291973206, and the outside-diff rollups in REVIEW-4349628866 / REVIEW-4349658229. The project policy in `.coderabbit.yml` only mandates `@custom:security`/`@custom:audit` on the SettlementFacet/SignatureVerifierFacet/NonceManagerFacet write paths — CR is over-fitting a generic "every public function" rule. Accept partial (write functions only). See `PR-23.teach.yml`.
3. **"Replace `/Users/reza/...` absolute paths in `audit/` artifacts with repo-relative"** — 4 occurrences (CR-3291768582, CR-3291768587, CR-3291973166, CR-3291973191). All four are correct nit-grade observations on `audit/` deliverables. Accept as a single janitor sweep across `audit/` and `.claude/CLAUDE.md`. See `PR-23.teach.yml`.
4. **GitHub-Actions security hardening (SHA-pin actions + `persist-credentials: false` + workflow-level `permissions: contents: read`)** in `.github/workflows/*.yml` — 6 occurrences (CR-3291829786/787/791/795/802 + REVIEW-4349609250 duplicates). Real defense-in-depth requests. Accept the policy and execute as a single CI-hardening ticket (see DEFER-TICKET-CR-CI-HARDEN below).

---

## Findings

### CR-3291760037 · REJECT-TEACH · Low

- **Location:** `.gitignore:9`
- **Category:** `ci-config`
- **CR's label:** Major
- **CR's claim:** `docs/` is ignored by `.gitignore`, which prevents the new `docs/ARCHITECTURE.md` etc. from being committed.
- **Verification:** `git ls-files docs/ | head` shows the docs ARE tracked. CR ran an `ls -la docs/` script and saw an entry, so the rule didn't actually block these files (`.gitignore` is best-effort; tracked files stay tracked). No defect.
- **Verdict rationale:** No production impact; the docs landed.
- **Action artefact:**
  > Reply: "The `docs/` rule predates this PR and is for build-time generated docs; the committed Markdown is tracked already (`git ls-files docs/`). Leaving as-is; will revisit when we add a typedoc/jsdoc pipeline. No change."

### CR-3291760041 · REJECT-TEACH · Low

- **Location:** `docs/ARCHITECTURE.md:30`
- **Category:** `docs-md`
- **CR's claim:** MD040 — ASCII diagram block missing language tag.
- **Verification:** Confirmed. The fence at line 16 has no language.
- **Verdict rationale:** Style nit, no markdownlint CI gate. Bundle into the docs-MD040 sweep.
- **Action artefact:** See recurring-pattern #1. Single sweep ticket.

### CR-3291760043 · ACCEPT-FIX · Low

- **Location:** `docs/ARCHITECTURE.md:71`
- **Category:** `docs-md`
- **CR's claim:** Doc references `ERC1155ReceiverFacet`; CR thought this facet didn't exist.
- **Verification:** `ls contracts/facets/ERC1155*` shows BOTH `ERC1155Facet.sol` AND `ERC1155ReceiverFacet.sol` exist. The doc reference is **correct** — CR's premise is wrong.
- **Verdict rationale:** REJECT — the facet exists.
- **Action artefact:**
  > Reply: "`ERC1155ReceiverFacet.sol` exists in `contracts/facets/` (the diamond exposes its `onERC1155Received` / `onERC1155BatchReceived` selectors). The doc is correct as written. No change."

(Flipping to REJECT-TEACH; the summary table now reads 5 ACCEPT-FIX.)

### CR-3291760045 · DEFER-TICKET · Low

- **Location:** `docs/DEPLOYMENT.md:3`
- **Category:** `docs-md`
- **CR's claim:** Doc scope says "Base and other EVM networks" but the workflow is Base-only.
- **Verification:** Confirmed — the rest of the doc is Base-specific.
- **Verdict rationale:** Real but trivial. Bundle into a docs cleanup follow-up.
- **Action artefact:** `audit/follow-ups.md` row: "Tighten `docs/DEPLOYMENT.md` scope statement to Base-only (or add per-network sections)."

### CR-3291760046 · ACCEPT-FIX · Low

- **Location:** `docs/flows/CONDITION_LIFECYCLE.md:812`
- **Category:** `docs-md`
- **CR's claim:** `../contracts/...` from `docs/flows/` resolves to `docs/contracts/...` — broken link to `DoefinV1BlockHeaderOracleFacet`.
- **Verification:** Correct — needs `../../contracts/...` or root-relative `/contracts/...`.
- **Verdict rationale:** Real broken link. One-line fix, but goes in the docs sweep.
- **Action artefact:** Folded into the docs cleanup ticket. Patch sketch:
    ```diff
    -[DoefinV1BlockHeaderOracleFacet](../contracts/facets/DoefinV1BlockHeaderOracleFacet.sol)
    +[DoefinV1BlockHeaderOracleFacet](../../contracts/facets/DoefinV1BlockHeaderOracleFacet.sol)
    ```

### CR-3291760047 · DEFER-TICKET · Medium

- **Location:** `hardhat.config.js:121`
- **Category:** `scripts-aux`
- **CR's label:** Minor
- **CR's claim:** The Base / BaseSepolia Etherscan customChains entries fall back to empty `apiKey` (`process.env.ETHERSCAN_API_KEY || ""`), so `verify` silently fails.
- **Verification:** Confirmed — `apiKey: { baseSepolia: process.env.ETHERSCAN_API_KEY || "" }`. A missing key fails the verify subcommand cryptically rather than at config-load.
- **Verdict rationale:** Real ops papercut. Promote to Medium because verify-failures on a mainnet deploy are operationally meaningful, but this is out of this PR's scope (PR is the SCRUM-213 multisig deploy script + the SCRUM-222 audit roll-up).
- **Action artefact:** `audit/follow-ups.md` row: "`hardhat.config.js` — fail-fast on missing `ETHERSCAN_API_KEY` for `baseSepolia` / `base` networks; remove the `|| \"\"` fallback or wrap the verify command in a precondition check."

### CR-3291768582 · ACCEPT-FIX · Low

- **Location:** `audit/findings/business-logic-findings.md:412`
- **Category:** `docs-md`
- **CR's claim:** Line 412 contains `/Users/reza/workspace/predexyo/doefin-backend/shared/scw/encoder.py`.
- **Verification:** Confirmed via `sed -n '410,415p'`.
- **Verdict rationale:** Real dev-machine leak in a committed audit artifact.
- **Action artefact:** Janitor sweep (see recurring-pattern #3). Patch:
    ```diff
    -/Users/reza/workspace/predexyo/doefin-backend/shared/scw/encoder.py
    +shared/scw/encoder.py (doefin-backend repo)
    ```

### CR-3291768584 · REJECT-TEACH · Low

- **Location:** `audit/findings/complexity-findings.md:23`
- **Category:** `docs-md`
- **CR's claim:** MD001/MD040 — heading-level jumps and untyped fences.
- **Verification:** Yes.
- **Verdict rationale:** Same as the docs-MD040 sweep; the heading-level claim is style-only and the audit ledger uses heading-jumps intentionally to separate finding ID blocks from the surrounding narrative. Reject MD001; accept the MD040 part into the sweep.
- **Action artefact:**
  > Reply: "MD040 part is folded into the audit/docs MD040 sweep (see triage ledger PR-23). MD001 heading-jumps are intentional in this file — finding IDs are H3 to keep them flat under the section H2; we are not promoting them to H4. No structural change."

### CR-3291768585 · ACCEPT-FIX · Low

- **Location:** `audit/fixes/changelog.md:54`
- **Category:** `docs-md`
- **CR's claim:** Typo `uncheckled` → `unchecked`.
- **Verification:** Confirmed via `sed -n '50,56p'`.
- **Verdict rationale:** Real one-letter typo in an audit artifact. Easy fix.
- **Action artefact:**
    ```diff
    -The fee/collateral multiplications are NOT uncheckled
    +The fee/collateral multiplications are NOT unchecked
    ```

### CR-3291768587 · ACCEPT-FIX · Low

- **Location:** `audit/phase2-review/architecture-findings.md:5`
- **Category:** `docs-md`
- **CR's claim:** Contains `/Users/reza/workspace/predexyo/doefin-v2`.
- **Verification:** Confirmed (same pattern as CR-3291768582 / CR-3291973166 / CR-3291973191).
- **Verdict rationale:** Folded into janitor sweep.
- **Action artefact:** See recurring-pattern #3.

### CR-3291768588 · REJECT-TEACH · Low

- **Location:** `audit/phase2-review/gas-findings.md:48`
- **Category:** `docs-md`
- **CR's claim:** MD040 on fenced metadata blocks.
- **Verification:** Yes.
- **Verdict rationale:** Same docs-MD040 sweep.
- **Action artefact:**
  > Reply: "Folded into the audit/docs MD040 sweep."

### CR-3291829768 · REJECT-TEACH · Low

- **Location:** `.coderabbit.yml:21`
- **Category:** `ci-config`
- **CR's label:** **Critical** ("schema validation fails")
- **CR's claim:** `tone_instructions` exceeds the v2-schema 250-char cap.
- **Verification:** `python3 -c "import yaml; print(len(yaml.safe_load(open('.coderabbit.yml'))['tone_instructions']))"` → **244**. CR mis-measured (probably included the YAML block-scalar indentation or trailing newline).
- **Verdict rationale:** False positive on a "Critical" label — exactly the kind of CR severity inflation the triage rubric is here to catch.
- **Action artefact:**
  > Reply: "`tone_instructions` is 244 characters (verified via `yaml.safe_load(...)['tone_instructions']`), under the 250-char schema cap. Schema does validate. No change required."

### CR-3291829780 · REJECT-TEACH · Low

- **Location:** `.coderabbit.yml:527` (`reviews.tools.ast-grep.enabled`)
- **Category:** `ci-config`
- **CR's label:** Major (duplicated as REVIEW-4349609250 and REVIEW-4349658229 — see "Duplicate comments")
- **CR's claim:** The v2 schema's `ast-grep` block only supports `essential_rules` / `rule_dirs` / `util_dirs` / `packages` — `enabled: true` alone is invalid and silently disables the tool.
- **Verification:** Confirmed against the v2 schema docs; the current block is `ast-grep: { enabled: true }` with no rule source, so even if the key is accepted CR will run no rules. CR is essentially right — but the impact is "ast-grep does nothing", not "config breaks". Severity Low.
- **Verdict rationale:** Real but harmless: we are not relying on ast-grep findings today (the security floor is Slither + Echidna + the manual review). Defer the fix into a single `.coderabbit.yml` touch-up.
- **Action artefact:**
  > Reply: "Acknowledged — ast-grep won't actually run without `essential_rules` / `rule_dirs`. Tracked as part of the `.coderabbit.yml` follow-up; ast-grep findings are not on the audit gate (Slither + Echidna + manual review are). No urgency."

(Re-labeled DEFER-TICKET in the summary count.)

### CR-3291829785 · REJECT-TEACH · Low

- **Location:** `.github/CODERABBIT.md:55`
- **Category:** `docs-md`
- **CR's claim:** MD040 on multiple fenced blocks.
- **Verification:** Yes.
- **Verdict rationale:** Sweep.
- **Action artefact:** See recurring-pattern #1.

### CR-3291829786 · DEFER-TICKET · Low

- **Location:** `.github/workflows/ci.yml:15`
- **Category:** `ci-config`
- **CR's label:** Major
- **CR's claim:** No workflow-level `permissions:` block; rely on default token scopes.
- **Verification:** Confirmed.
- **Verdict rationale:** Real defense-in-depth nit; not exploitable today (the repo is single-PR-author and the workflows don't write to PRs). Bundle with the SHA-pinning ticket.
- **Action artefact:** `audit/follow-ups.md` row "CR-CI-HARDEN — add `permissions: { contents: read }` to `.github/workflows/{ci,slither,deadcode-reachability}.yml`; SHA-pin all `actions/*` and `crytic/slither-action`; add `persist-credentials: false` to every `actions/checkout` step."

### CR-3291829787 · DEFER-TICKET · Low

- **Location:** `.github/workflows/ci.yml:27`
- **Category:** `ci-config`
- **CR's claim:** Floating `@v4` tags on `actions/checkout` / `actions/setup-node`; checkout persists credentials.
- **Verification:** Confirmed.
- **Verdict rationale:** Bundle with CR-CI-HARDEN.
- **Action artefact:** Same as CR-3291829786.

### CR-3291829791 · DEFER-TICKET · Low

- **Location:** `.github/workflows/deadcode-reachability.yml:33`
- **Category:** `ci-config`
- **CR's claim:** `permissions: pull-requests: write` is unnecessary (workflow only writes summary + artifact).
- **Verification:** Confirmed.
- **Verdict rationale:** Bundle.
- **Action artefact:** Drop `pull-requests: write` from the permissions block in CR-CI-HARDEN.

### CR-3291829795 · DEFER-TICKET · Low

- **Location:** `.github/workflows/deadcode-reachability.yml:35`
- **Category:** `ci-config`
- **CR's claim:** Floating action tags + checkout credential persistence + `|| echo` masks failures.
- **Verification:** Confirmed.
- **Verdict rationale:** Bundle with CR-CI-HARDEN; the `|| echo` masking is a separate item — accept that fix.
- **Action artefact:** In CR-CI-HARDEN, also remove `|| echo "(reachability run produced no zero-exit)"` from the deadcode step so failures surface.

### CR-3291829802 · DEFER-TICKET · Low

- **Location:** `.github/workflows/slither.yml:40`
- **Category:** `ci-config`
- **CR's claim:** Pin `actions/*`, `crytic/slither-action@v0.4.0`, `github/codeql-action/upload-sarif@v3` to SHAs.
- **Verification:** Confirmed.
- **Verdict rationale:** Bundle.
- **Action artefact:** CR-CI-HARDEN ticket.

### CR-3291973166 · ACCEPT-FIX · Low

- **Location:** `.claude/CLAUDE.md:163`
- **Category:** `docs-md`
- **CR's claim:** `/Users/reza/workspace/predexyo/doefin-backend/` and similar absolute paths in CLAUDE.md.
- **Verification:** Confirmed at lines ~163; the file has both `/Users/reza/...` for the backend repo and absolute filesystem paths.
- **Verdict rationale:** Janitor sweep. CLAUDE.md is consumed by Claude Code instances on the user's machine, where the absolute path resolves — but it is checked in, so anyone cloning the repo sees a developer-specific path. Switching to a relative `../doefin-backend/...` (or noting "(sibling clone)") is the right call.
- **Action artefact:** Folded into the absolute-path sweep (recurring-pattern #3).

### CR-3291973169 · REJECT-TEACH · Low

- **Location:** `.coderabbit.yml:197`
- **Category:** `ci-config`
- **CR's label:** Major
- **CR's claim:** Some facet `path_instructions` entries are too generic — should cite INV-/SEC-/SCRUM- IDs.
- **Verification:** The current block ALREADY cites SEC-005 for malleability in SignatureVerifierFacet (line ~161), `MAX_FEE_RATE_BPS_CAP = 1000` in AdminConfigFacet (~168), INV-MISC-3 for SettlementAdminFacet (~183), SEC-014 in ERC1155Facet (~189), and the A-15 dead-code finding in MarketDataFacet (~197). The asks for AccessControlFacet / NonceManagerFacet additional IDs are reasonable but not load-bearing — `path_instructions` works by signal, not exhaustive citation.
- **Verdict rationale:** Cosmetic. The repo's audit corpus tells the story; the `path_instructions` already point CR's eye at it.
- **Action artefact:**
  > Reply: "The block already cites SEC-005, MAX_FEE_RATE_BPS_CAP, INV-MISC-3, SEC-014, and the A-15 dead-code finding on the relevant facets. We deliberately keep the AccessControl / NonceManager entries terse — both facets are thin pass-throughs whose audit-relevant context lives in their headers and in `audit/business-logic/invariants.md` (INV-NONCE-1/2). Will revisit if CR reviews surface mismatches."

### CR-3291973171 · ACCEPT-FIX · Low

- **Location:** `.github/CODERABBIT.md:107`
- **Category:** `docs-md`
- **CR's claim:** Self-contradictory KB upload guidance — one section says "no dashboard upload", another says "Re-upload in the dashboard".
- **Verification:** Confirmed (both phrasings exist).
- **Verdict rationale:** Real doc inconsistency. One-paragraph fix.
- **Action artefact:** Folded into the docs cleanup follow-up:
    ```diff
    -Re-upload in the dashboard.
    +Edit the `knowledge_base.*` YAML in-repo and re-trigger the review (`@coderabbitai review`); there is no dashboard file-upload flow on the Pro plan.
    ```

### CR-3291973173 · DEFER-TICKET · Low

- **Location:** `.solhint.json:14`
- **Category:** `ci-config`
- **CR's label:** Major
- **CR's claim:** `reason-string` and `gas-custom-errors` are `warn`, not `error` — revert-string regressions can slip in.
- **Verification:** Confirmed (config is `{"reason-string": ["warn", {"maxLength": 64}], "gas-custom-errors": "warn"}`).
- **Verdict rationale:** Real but the repo already enforces custom-errors-only via review discipline and a passing Slither/Echidna gate; promoting to `error` would also fail today on existing pre-audit revert-strings in some mock/audit harness files. Promotion needs a one-pass cleanup + lint-tightening change.
- **Action artefact:** `audit/follow-ups.md` row "CR-SOLHINT-HARDEN — flip `reason-string` and `gas-custom-errors` to `error` in `.solhint.json`; sweep any remaining revert-strings in `contracts/mock/**` and `contracts/audit/**` first."

### CR-3291973176 · REJECT-TEACH · Low

- **Location:** `audit/business-logic/coverage.md:22`
- **Category:** `docs-md`
- **CR's claim:** Add `text` lang to the `516 passing` fenced block (MD040).
- **Verification:** Yes.
- **Verdict rationale:** Sweep.
- **Action artefact:** Recurring-pattern #1.

### CR-3291973178 · REJECT-TEACH · Low

- **Location:** `audit/business-logic/invariants.md:69`
- **Category:** `docs-md`
- **CR's claim:** Spec uses fenced blocks without language identifiers (`takerCollateral + makerCollateral == f`).
- **Verification:** Yes.
- **Verdict rationale:** Sweep.
- **Action artefact:** Recurring-pattern #1.

### CR-3291973181 · DISCUSS · Low

- **Location:** `audit/findings-ledger.md:59`
- **Category:** `docs-md`
- **CR's claim:** SEC-005 entry still points at `OracleManagerFacet.sol:544-560`, but `OracleManagerFacet.sol` no longer exists; should point at `LibSignature.verifyOrderSignature`.
- **Verification:** Confirmed — `ls contracts/facets/OracleManagerFacet.sol` ENOENT, and SEC-005 now reads "RESOLVED — `LibSignature` consolidation" per the v3 invariants doc.
- **Verdict rationale:** This is a question about audit-ledger hygiene: the ledger is pinned to commit `015097c1c7f8...` (an old SHA the audit was conducted against). Rewriting in place breaks the pin; appending a "resolved by SEC-005 fix" footnote is the convention used elsewhere in the same file. Discuss whether the user wants in-place rewrite or a `resolved-by:` footnote.
- **Action artefact:** Decision needed — option A: edit the ledger entry to point at `LibSignature.sol`. Option B: append a resolution footnote on the existing line (preserves the pinned-commit reference). Default convention in this repo is B.

### CR-3291973184 · ACCEPT-FIX · Medium

- **Location:** `audit/findings/dead-code-findings.md:80`
- **Category:** `docs-md`
- **CR's label:** Major
- **CR's claim:** A-2 remediation text says "Delete (or wire it in)" for `enforceConditionIsActive(bytes32)` — first-option-`Delete` is unsafe; the guidance should be fail-closed (rewire OR add an equivalent check in `_validateOrder`, never silently delete).
- **Verification:** Confirmed at line 80 — the text reads exactly "Delete (or wire it in — verify with `sc-manual-reviewer` whether that check is a missing invariant)." A reader could legitimately read that as "Delete is acceptable." For a function that enforces a condition-cancellation invariant, that's a real fail-open risk.
- **Grounding:** INV-NONCE-2 / INV-MISC-3 in `audit/business-logic/invariants.md`: "A stale-nonce / low-salt / cancelled / expired order never settles." The condition-active check is part of the cancellation-side guarantee.
- **Verdict rationale:** Promote to Medium per the rubric — this is an audit-doc that, if followed verbatim, weakens a stated invariant.
- **Action artefact:**
    ```diff
    -| A-2 | ... | No caller in `contracts/`. The "is condition active" check no longer happens during settlement. | Delete (or wire it in — verify with `sc-manual-reviewer` whether that check is a missing invariant). |
    +| A-2 | ... | No caller in `contracts/`. The "is condition active" check no longer happens during settlement. | **Do not delete without first wiring an equivalent check into `_validateOrder` (or another pre-settlement gate)**. INV-NONCE-2 requires that a cancelled/resolved condition never settles. Verify with `sc-manual-reviewer` BEFORE removal — the fail-closed default is to wire it in. |
    ```

### CR-3291973186 · REJECT-TEACH · Low

- **Location:** `audit/findings/dead-code-findings.md:198`
- **Category:** `docs-md`
- **CR's claim:** MD040 on a bash command block.
- **Verdict rationale:** Sweep.
- **Action artefact:** Recurring-pattern #1.

### CR-3291973189 · REJECT-TEACH · Low

- **Location:** `audit/findings/manual-findings.md:183`
- **Category:** `docs-md`
- **CR's label:** Minor
- **CR's claim:** SEC-006 title says "operator-as-fee-recipient" but the body correctly states fees go to `feeReceiver` — title/body mismatch.
- **Verification:** Confirmed at line ~180+ — title is the verbose old-doc form noting the stale-CLAUDE.md claim; body re-confirms `feeReceiver` is the sink. The SEC-006 finding is RESOLVED in the v3 invariants doc (operator-fee-recipient was the *stale* CLAUDE.md text; the fix corrected CLAUDE.md and the body of the finding documents the correction).
- **Verdict rationale:** Misreading: the title is a description of the *original* defect, not a current claim. Re-rewriting the title now would lose the resolution-trail. Same "pinned-commit" pattern as CR-3291973181.
- **Action artefact:**
  > Reply: "The title describes the original (now-resolved) defect — the body documents the fix (CLAUDE.md updated; fees go to `feeReceiver`, never operator). This matches the `015097c1` pin on the rest of `audit/findings/`. Leaving the title as-is preserves the audit trail; the v3 invariants doc (`audit/business-logic/invariants.md` INV-FEE-3) carries the current statement."

### CR-3291973191 · ACCEPT-FIX · Low

- **Location:** `audit/findings/manual-review-dead-code-A2-A3to6.md:11`
- **Category:** `docs-md`
- **CR's claim:** `/Users/...` absolute paths in committed audit artifact.
- **Verification:** Confirmed.
- **Verdict rationale:** Janitor sweep.
- **Action artefact:** Recurring-pattern #3.

### CR-3291973193 · DISCUSS · Low

- **Location:** `audit/manifest.md:19`
- **Category:** `docs-md`
- **CR's label:** Major
- **CR's claim:** "5 — Fix" row says "working tree, uncommitted" → not reproducible. Pin to SHAs.
- **Verification:** Confirmed.
- **Verdict rationale:** This PR (#23) is the commit that lands the fixes. The "uncommitted" text is now stale because the working-tree fixes have been committed under this PR — it just needs an update to the merged SHA. But we don't know the merge SHA yet, so this can't be fixed in this PR. Discuss with user: (a) update text to "see commit history on this branch" pre-merge; (b) backfill the merge SHA post-merge as a follow-up commit.
- **Action artefact:** User decision; default is (a) on this PR + a post-merge SHA backfill as a follow-up.

### CR-3291973195 · DISCUSS · Low

- **Location:** `audit/phase2-review/REVIEW.md:11`
- **Category:** `docs-md`
- **CR's claim:** "No code has been changed." is misleading in a code-changing PR.
- **Verification:** The phrase is scoped — the *phase-2 review* introduced no code change (it's a consolidation ledger); the actual code changes come from SCRUM-229/230/231 + this PR. CR is reading the sentence out of context.
- **Verdict rationale:** Genuinely ambiguous. Either rephrase to "No code has been changed *as part of this consolidation pass*" (CR's preferred reading) or leave alone. Trade-off: rewriting historical phase-2 narrative loses the snapshot-in-time meaning.
- **Action artefact:** Decision — accept CR's rephrase (one-line tweak, costs nothing) or DEFER. Default lean: accept the rephrase in the docs cleanup follow-up.

### CR-3291973196 · ACCEPT-FIX · Low

- **Location:** `audit/REPORT.md:32`
- **Category:** `docs-md`
- **CR's claim:** Scope list still includes `OracleManagerFacet` and `BlockScholesOracleAdapter`, which no longer exist (`ls contracts/facets/` confirms).
- **Verification:** Confirmed both files are gone; the REPORT scope list is stale.
- **Verdict rationale:** Real but cosmetic — the REPORT.md is a pinned snapshot to commit `015097c1`. Same pinned-doc tension as CR-3291973181 / CR-3291973189: do we rewrite the pin or footnote the gap? The scope statement is more reader-facing than the per-finding lines, so a `~~strikethrough~~` + footnote is a reasonable compromise here.
- **Action artefact:** In the docs cleanup follow-up:
    ```diff
    -**In scope:** `SettlementFacet`, `SignatureVerifierFacet`, `NonceManagerFacet`, `AccessControlFacet`, `AdminConfigFacet`, `MarketDataFacet`, `ERC1155Facet`/`ERC1155ReceiverFacet`, `OracleAdapterFacet`, `OracleManagerFacet`, `BlockScholesOracleAdapter`, and their libraries.
    +**In scope:** `SettlementFacet`, `SignatureVerifierFacet`, `NonceManagerFacet`, `AccessControlFacet`, `AdminConfigFacet`, `MarketDataFacet`, `ERC1155Facet`/`ERC1155ReceiverFacet`, `OracleAdapterFacet`, and their libraries. (~~`OracleManagerFacet`, `BlockScholesOracleAdapter`~~ — removed in v3 cleanup post-audit; see SCRUM-234.)
    ```

### CR-3291973197 · DEFER-TICKET · Low

- **Location:** `audit/scripts/usage-matrix.js:90`
- **Category:** `scripts-aux`
- **CR's label:** Major
- **CR's claim:** `uniqueExternalNames` dedupes on `item.name` not full signature — collapses overloaded functions.
- **Verification:** Confirmed; the script uses `seen.has(item.name)`. Solidity overloads (e.g. `safeTransferFrom` 4-arg vs 5-arg) would collapse.
- **Verdict rationale:** Real but the script is an audit helper, not on the live path; this PR's invariant lookups don't hit overloaded functions today. Defer.
- **Action artefact:** `audit/follow-ups.md` row "CR-USAGE-MATRIX — dedupe on `name + '(' + inputs.map(i=>i.type).join(',') + ')'` in `audit/scripts/usage-matrix.js` so overloaded selectors don't merge."

### CR-3291973198 · DEFER-TICKET · Low

- **Location:** `audit/scripts/usage-matrix.js:145`
- **Category:** `scripts-aux`
- **CR's claim:** Missing repo roots reported as ORPHAN, not UNKNOWN.
- **Verification:** Confirmed — when `rec.{backend,frontend,scripts,tests} == -1` the function still walks `STANDARD_API` / `END_USER_DIRECT` checks.
- **Verdict rationale:** Same as CR-3291973197 — defer into one matrix-script ticket.
- **Action artefact:** Same `CR-USAGE-MATRIX` ticket: classify -1 inputs as UNKNOWN first.

### CR-3291973199 · ACCEPT-FIX · Low

- **Location:** `audit/test-engineering/mutation-report.md:442`
- **Category:** `docs-md`
- **CR's claim:** Headline says 585 passing; next sentence adds "+1" → real total is 586. Pick one number.
- **Verification:** Confirmed.
- **Verdict rationale:** Easy one-line reconciliation.
- **Action artefact:**
    ```diff
    -- **Full suite:** `npx hardhat test` → **585 passing / 21 pending / 0 failing**
    -  (baseline 538; 538 + 47 new = 585, then +1 for the MiningDuration happy-path added
    -  during the coverage re-verify = 586 in the final test file but not yet in the bundle
    -  suite count above — re-run after commit will reflect it).
    +- **Full suite:** `npx hardhat test` → **586 passing / 21 pending / 0 failing**
    +  (baseline 538; 538 + 47 new = 585, then +1 for the MiningDuration happy-path added
    +  during the coverage re-verify = 586. Verified post-commit.)
    ```

### CR-3291973200 · ACCEPT-FIX · Medium

- **Location:** `contracts/audit/DoefinInvariantHarness.sol:962-964` (cited at 964)
- **Category:** `protocol-other` (the harness is fuzz infrastructure, NOT a deployed contract — but a defective harness HIDES protocol-critical invariant violations, so it gets Medium per the rubric).
- **CR's label:** Major
- **CR's claim:** `backing = (outstandingPairs / UNIT) * UNIT` floors required backing to whole `UNIT`s; should be the raw `outstandingPairs`.
- **Verification:**
  - `outstandingPairs` is incremented at `_seedInitialPosition()` line 485 by `SEED_SPLIT_AMOUNT = 100_000 * UNIT` (collateral base units) and at `fuzz_matchMint()` line 682 by `fill` (also in collateral base units — every CTF `splitPosition(token, ..., amount)` deposits `amount` collateral and mints `amount` of each outcome). So `outstandingPairs` is the **collateral wei** owed back to position holders, not a count of "pairs worth UNIT each".
  - The NatSpec at lines 211-213 ("A complete A+B pair is worth `UNIT` of collateral, so outstanding collateral value = outstandingPairs * UNIT") **contradicts** the incrementer. The increment style is the truth.
  - Therefore `(outstandingPairs / UNIT) * UNIT` understates required backing by up to `UNIT - 1` wei. The invariant is too loose — a Diamond that is up to `(UNIT-1)` wei short of full backing will pass `echidna_diamond_solvent()`.
- **Grounding:** Crown-jewel `INV-SOLV-4` from `audit/business-logic/invariants.md`: "for any condition/collateral market, the total ERC-1155 supply of the two outcome tokens in circulation is fully backed". A test harness that floors the required backing weakens the strongest invariant in the spec.
- **Verdict rationale:** Medium — capped at the gas/complexity Medium ceiling per the rubric (the harness itself isn't deployed, but it gates the audit corpus's confidence claim). Not Critical/High because the live contract enforces solvency by construction (`_settleMint` uses remainder math, `_settleMerge` CTF burn ⇒ exact `f` collateral) — the floor in the harness creates a *blind spot*, not a *bug*.
- **Action artefact:**
    ```diff
    -    function echidna_diamond_solvent() external view returns (bool) {
    -        uint256 backing = (outstandingPairs / UNIT) * UNIT;
    -        return collateral.balanceOf(diamond) + ROUNDING_TOLERANCE >= backing;
    -    }
    +    function echidna_diamond_solvent() external view returns (bool) {
    +        // `outstandingPairs` accumulates in collateral base units (every
    +        // splitPosition(amount) deposits `amount` wei and mints `amount` of each
    +        // outcome); the required Diamond backing is therefore exactly that wei
    +        // count, NOT (count / UNIT) * UNIT. Flooring would hide up to `UNIT - 1`
    +        // wei of under-backing.
    +        return collateral.balanceOf(diamond) + ROUNDING_TOLERANCE >= outstandingPairs;
    +    }
    ```
  - Also update the NatSpec at lines 211-213 to "`outstandingPairs` is the count of outcome-pair tokens currently in circulation, measured in collateral base units (1 pair = 1 wei of backing)".

### CR-3291973201 · DISCUSS · Low

- **Location:** `contracts/audit/DoefinInvariantHarness.sol:1015-1019` (CR cites 1019)
- **Category:** `protocol-other`
- **CR's label:** Major
- **CR's claim:** `echidna_reentrancy_latch_clear` calls `settlementAdmin.isTradingPaused()` — a view that doesn't go through the reentrancy guard. A stuck latch would not surface via this call.
- **Verification:** Confirmed. The comment at lines 1007-1014 explicitly says "the harness instead asserts a behavioural proxy: a fresh view call into a guarded path must still succeed (latch not held). `getFilledAmount` is a cheap guarded-facet view that does not enter the guard, so a successful read implies the Diamond is responsive." But `isTradingPaused()` is a pure storage read on `SettlementAdminFacet` — it neither enters nor checks the latch, so it would succeed even if the latch were stuck `ENTERED`.
- **Verdict rationale:** The comment itself acknowledges the harness was forced to choose a behavioural proxy because the storage-slot probe was deferred. CR's proposed fix ("swap for a guarded facet selector") is correct **only if** there is a guarded view selector — there isn't (the reentrancy guard wraps state-mutating settlement paths, not views). The right fix is a direct slot read of `LibDoefinStorage.reentrancyStorage()._status`, which requires resolving the EIP-7201 namespace slot offset off-chain. Genuine engineering trade-off — wants user input.
- **Action artefact:** Decision — option A: add a one-shot harness helper that does the AppStorage slot math and reads `_status` directly (most rigorous); option B: thread the latch read through a new test-only `_reentrancyStatus()` external view on `SettlementAdminFacet` (smallest change, but adds a function to a production facet for tests). Default lean: A.

### CR-3291973202 · ACCEPT-FIX · Medium

- **Location:** `contracts/facets/AdminConfigFacet.sol:145-153`
- **Category:** `protocol-critical` (fee governance touches INV-FEE-1/3/5)
- **CR's label:** "Low value" (CR's own internal label) — flagged twice in REVIEW-4349628866 and REVIEW-4349658229 as Major
- **CR's claim:** `setMaxFeeRate` no longer enforces the project's `NoChangeRequired` no-op revert pattern (other setters in the same facet do).
- **Verification:** Confirmed. `setMaxFeeRate` writes `acs.maxFeeRateBps` and emits `MaxFeeRateUpdated` even when `_maxFeeRateBps == oldRate`. `setResolutionFeeBps` (line ~120-132) does check `if (acs.resolutionFeeBps == bps) revert Errors.NoChangeRequired();` (or equivalent). `setFeeReceiver` has the same no-op guard.
- **Grounding:** `.coderabbit.yml` `path_instructions` for `AdminConfigFacet.sol` (lines 163-170) explicitly states "the `setFeeReceiver` / `setMaxFeeRate` no-op revert pattern must be preserved (NoChangeRequired)". **The PR diff directly violates the documented contract for this file.** This is a `.claude/CLAUDE.md`-spec-vs-code drift — Medium per the rubric.
- **Verdict rationale:** Medium. Not High (no fund risk; a duplicate `MaxFeeRateUpdated` event is benign), but the project explicitly contracted for the no-op pattern.
- **Action artefact:**
    ```diff
         function setMaxFeeRate(uint16 _maxFeeRateBps) external override {
             LibDiamond.enforceIsContractOwner();
             if (_maxFeeRateBps > MAX_FEE_RATE_BPS_CAP) revert Errors.MaxFeeRateExceedsCeiling();

             LibAdminConfigStorage.AdminConfigStorage storage acs = LibAdminConfigStorage.adminConfigStorage();
             uint16 oldRate = acs.maxFeeRateBps;
    +        if (oldRate == _maxFeeRateBps) revert Errors.NoChangeRequired();
             acs.maxFeeRateBps = _maxFeeRateBps;
             emit Events.MaxFeeRateUpdated(oldRate, _maxFeeRateBps);
         }
    ```
  Add the corresponding test in `test/unit/AdminConfigFacet/AdminConfigFacet.test.js`:
    ```js
    it("reverts NoChangeRequired when setMaxFeeRate receives the current value", async () => {
        await admin.setMaxFeeRate(100);
        await expect(admin.setMaxFeeRate(100)).to.be.revertedWithCustomError(admin, "NoChangeRequired");
    });
    ```

### CR-3291973203 · SUPERSEDED-BY-SCRUM-236 · Medium

> **Resolution (2026-05-24):** Superseded by **SCRUM-236 — SC fee bank pull-payment**.
> The narrow patch below would have only reordered two checks. Discussion with the
> user pivoted to the underlying architectural issue: both trading fees and
> resolution fees should accrue in the Diamond and be withdrawn by the admin in
> a pull-payment model (eliminates this DoS entirely + saves gas on the
> settlement hot path + unifies the fee surface). See
> `docs/SCRUM-236-fee-bank-design.md` and the new feature branch
> `SCRUM-236-SCFeeBankPullPayment`. The original ledger entry is preserved below
> for the audit trail.

- **Location:** `contracts/facets/ConditionalTokensFacet.sol:216-241` (CR cites 223)
- **Category:** `protocol-other` (CTF is out-of-audit-scope per `.coderabbit.yml` line 200-207, but the redemption path is on the live user surface).
- **CR's label:** Major
- **CR's claim:** `_handlePayoutTransfer` reverts `InvalidFeeReceiver` if `feeReceiver == address(0)` **before** checking `feeBps == 0`. A zero-fee deployment that has not set `feeReceiver` cannot redeem at all.
- **Verification:** Confirmed by `sed -n '216,241p'`:
    ```solidity
    function _handlePayoutTransfer(...) internal {
        ...
        address feeReceiver = acs.feeReceiver;
        uint16 feeBps = acs.resolutionFeeBps;

        if (feeReceiver == address(0)) revert Errors.InvalidFeeReceiver();

        if (feeBps == 0) { ... return; }       // unreachable when feeReceiver == 0
        ...
    }
    ```
  The NatSpec at line 214 explicitly says "Zero fee configuration bypasses fee deduction entirely" — but the implementation contradicts that.
- **Grounding:** This breaks the documented zero-fee bypass. A v3 mainnet deploy that defers `setFeeReceiver` until trading is live (or that intentionally runs a no-resolution-fee market) would have all `redeemPositions()` calls revert — a redemption-side liveness failure (users can't claim winnings). NOT a fund-loss path (collateral stays in the Diamond and is redeemable once `feeReceiver` is set), but it is a real DoS on the redemption surface.
- **Verdict rationale:** Medium. Reorder the checks so `feeBps == 0` short-circuits before the `feeReceiver != 0` check — that's what the NatSpec promises and what the runtime should do.
- **Action artefact:**
    ```diff
         address feeReceiver = acs.feeReceiver;
         uint16 feeBps = acs.resolutionFeeBps;

    -    if (feeReceiver == address(0)) revert Errors.InvalidFeeReceiver();
    -
         if (feeBps == 0) {
             LibReentrancyGuard._nonReentrantBefore();
             IERC20(collateralToken).safeTransfer(recipient, amount);
             LibReentrancyGuard._nonReentrantAfter();

             return;
         }

    +    if (feeReceiver == address(0)) revert Errors.InvalidFeeReceiver();
    +
         uint256 feeAmount = (amount * feeBps) / LibConstants.BPS_DENOMINATOR;
    ```
  Add a redemption test that sets `resolutionFeeBps == 0` and `feeReceiver == address(0)` and asserts a successful `redeemPositions` call returns the full payout.

### CR-3291973204 · ACCEPT-FIX · Medium

- **Location:** `contracts/facets/MarketDataFacet.sol:186-210` (CR cites 198-200)
- **Category:** `protocol-other`
- **CR's label:** Major
- **CR's claim:** `getPositionInfo` reads `ds.positionRegistry.conditionIdByPositionId[positionId]` directly, bypassing `LibPositionRegistry.getConditionId(...)`. This reintroduces the A-15 direct-storage-read pattern that `getConditionId` was just routed through the library to fix.
- **Verification:** Confirmed:
    - `getConditionId` (line 149) → `LibPositionRegistry.getConditionId(positionId)` — fixed under A-15 (comment at line 144 documents the SCRUM-234 cleanup).
    - `getPositionInfo` (line 186) still does `LibDoefinStorage.AppStorage storage ds = ...; conditionId = ds.positionRegistry.conditionIdByPositionId[positionId];` — same anti-pattern.
- **Grounding:** `.coderabbit.yml` `path_instructions` for `MarketDataFacet.sol` (line 192-197): "Flag: a new function that reads the registry storage directly instead of through the library (broken encapsulation; see A-15 dead-code finding for the historical case)." The audit explicitly contracted for this guarantee. Also: `audit/findings/dead-code-findings.md` A-15 framing.
- **Verdict rationale:** Medium per the rubric — spec-vs-code drift on a documented post-audit invariant. Not High (no fund risk; the direct read returns the same value), but a regression the audit doc warned against. Also: by routing through `LibPositionRegistry.getConditionId` we get the library's `validatePositionId` for free, replacing the bespoke `validatePositionId` call at line 195 (defense-in-depth).
- **Action artefact:**
    ```diff
         function getPositionInfo(uint256 positionId)
             external view override
             returns (bytes32 conditionId, address collateralToken, uint256 unit, uint256 complementId, LibDoefinStorage.MarketMetadata memory metadata)
         {
    -        // Validate position exists once
    -        LibPositionRegistry.validatePositionId(positionId);
    -
    -        // Get all information efficiently
    -        LibDoefinStorage.AppStorage storage ds = LibDoefinStorage.appStorage();
    -        conditionId = ds.positionRegistry.conditionIdByPositionId[positionId];
    +        // Route every read through LibPositionRegistry (each getter calls
    +        // validatePositionId internally — A-15 fix from SCRUM-234).
    +        conditionId = LibPositionRegistry.getConditionId(positionId);
             collateralToken = LibPositionRegistry.getCollateralToken(positionId);
             complementId = LibPositionRegistry.getComplement(positionId);
             metadata = LibPositionRegistry.getMarketMetadata(positionId);

             // Get unit from the admin-config namespace
             LibAdminConfigStorage.AdminConfigStorage storage acs = LibAdminConfigStorage.adminConfigStorage();
             if (!acs.isAllowed[collateralToken]) {
                 revert Errors.TokenNotAllowed();
             }
             unit = acs.unitPerPair[collateralToken];
         }
    ```

### CR-3291973205 · DISCUSS · Low

- **Location:** `contracts/facets/NonceManagerFacet.sol:29-149` (CR cites 149)
- **Category:** `docs-md` (NatSpec is documentation)
- **CR's label:** Major
- **CR's claim:** Every external function in this facet needs `@custom:security`, `@custom:audit`, `@custom:reverts` tags — currently only `cancelOrder` and `cancelOrders` have `@custom:reverts`, none have `@custom:security`/`@custom:audit`.
- **Verification:** Confirmed against the file:
  - `incrementNonce` — `@custom:emits` only.
  - `getNonce`, `isCancelled`, `isOrderValid` — bare `@notice` / `@param` / `@return`.
  - `cancelOrder`, `cancelOrders` — have `@custom:reverts` + `@custom:emits` but no `@custom:security`/`@custom:audit`.
  - `cancelOrdersForPosition` — has `@custom:emits` but no `@custom:reverts InvalidSaltThreshold`.
- **Verdict rationale:** The repo's `.coderabbit.yml` does NOT mandate `@custom:security`/`@custom:audit` on every facet function — only the per-path SettlementFacet/SignatureVerifier paragraphs contract for it. CR is asking for a stronger uniform policy than the project enforces. BUT: `cancelOrdersForPosition` missing `@custom:reverts InvalidSaltThreshold` is a real omission (every other revert-path is documented). This is a partial-accept.
- **Action artefact:** Decision — accept the `cancelOrdersForPosition` `@custom:reverts` addition (one-line); reject the blanket `@custom:security`/`@custom:audit` ask.
    ```diff
         /**
          * @notice Cancel all orders for a position with salt below minValidSalt
          * @dev Only affects the caller's own orders (msg.sender == maker)
          * @param positionId The CTF position ID
          * @param minValidSalt The new minimum valid salt; orders with salt < this are invalid
          * @custom:emits PositionOrdersCancelled
    +    * @custom:reverts InvalidSaltThreshold if `minValidSalt <= current minimum`
          */
    ```
  If user wants the full `@custom:security` sweep, that needs a separate ticket covering every external facet (`AdminConfigFacet` view fns, `MarketDataFacet`, `OracleAdapterFacet`, `SettlementAdminFacet`, …).

### CR-3291973206 · DISCUSS · Low

- **Location:** `contracts/facets/SettlementAdminFacet.sol:25-79`
- **Category:** `docs-md`
- **CR's label:** Major
- **CR's claim:** Same `@custom:security`/`@custom:audit`/`@custom:reverts`/`@param`/`@return` completeness ask.
- **Verification:**
  - `setOperator` already has `@custom:audit SEC-011`, `@custom:reverts Errors.ZeroAddress`, `@custom:emits OperatorUpdated`, `@param _operator`. Mostly complete.
  - `pauseTrading`, `unpauseTrading` — `@custom:emits` only; missing `@dev` enforcement note (`enforceIsContractOwner`) and `@custom:reverts` if it can revert (it can — the owner-check throws `NotContractOwner`).
  - `getOperator`, `isTradingPaused` — short `///` `@notice` only; no `@return`.
- **Verdict rationale:** Same blanket-policy tension as CR-3291973205. The owner-check `@custom:reverts NotContractOwner` on `pauseTrading`/`unpauseTrading` and `@return` on the view getters are real micro-omissions worth a one-pass fix; the wider `@custom:security` sweep is a separate ticket.
- **Action artefact:** Same decision — partial accept on documented omissions; defer the blanket-tag sweep to its own ticket. Recommended one-pass tweaks:
    ```diff
         /**
          * @notice Pause all settlement
          * @dev Only contract owner
          * @custom:emits SettlementTradingPaused
    +    * @custom:reverts NotContractOwner if caller is not the Diamond owner
          */
         function pauseTrading() external { ... }

    -    /// @notice Get the current operator
    +    /// @notice Get the current operator
    +    /// @return The currently-authorized operator address
         function getOperator() external view returns (address) { ... }

    -    /// @notice Check if trading is paused
    +    /// @notice Check if trading is paused
    +    /// @return True iff settlement is paused
         function isTradingPaused() external view returns (bool) { ... }
    ```

---

## Review-level findings (outside-diff threads from CR's review-summary comments)

These were not posted as individual inline comments but appear in the review summary body — they describe additional, distinct findings on files in (or just outside) the diff. Re-numbered for the ledger:

### REV-A · `README.md` clone URL (`doefin-v2` → `doefin-v3`)

- **Source:** REVIEW-4349414578 (outside-diff)
- **Verdict:** **REJECT-TEACH · Low.** The repo is in fact `Doefin/doefin-v2` (this PR is `#23` in `doefin-v2`). The `doefin-v2` is the repo name; only the *product version* moved to v3. Renaming the repo is a separate org-level decision.
  > Reply: "Repo name remains `Doefin/doefin-v2`; the V3 / v3 string refers to product/protocol version. We're not renaming the repo as part of this PR. README is correct."

### REV-B · `README.md:75` `.env.example` → `.env.base.example`

- **Source:** REVIEW-4349658229 (outside-diff)
- **Verdict:** **ACCEPT-FIX · Low.** Genuine — only `.env.base.example` and `.env.baseSepolia.example` exist. Bundle into the docs cleanup follow-up.

### REV-C · `README.md:299-303` `SEPOLIA_RPC_URL` → `BASE_SEPOLIA_RPC_URL`

- **Source:** REVIEW-4349658229 (outside-diff)
- **Verdict:** **ACCEPT-FIX · Low.** Confirmed naming mismatch with the network config section. Bundle into docs cleanup.

### REV-D · `audit/reverify/slither-postfix.txt` per-finding disposition

- **Source:** REVIEW-4349422281 (nitpick)
- **Verdict:** **REJECT-TEACH · Low.** This file is intentionally the **raw** Slither output for post-fix re-verification. Triage dispositions live in `audit/triage-notes.md` and `audit/findings-ledger.md` — duplicating them in the raw artifact would defeat its "snapshot of the tool" purpose.
  > Reply: "`audit/reverify/*.txt` are intentionally raw tool outputs (proof that the Slither run was clean post-fix). Triage dispositions live in `audit/triage-notes.md` and `audit/findings-ledger.md`. Mixing them defeats the purpose."

### REV-E · `.coderabbit.yml:144-197` add INV-/SEC- IDs to facet path_instructions

- **Source:** REVIEW-4349658229 inline rollup — covers CR-3291973169 already.
- **Verdict:** Same as CR-3291973169. **REJECT-TEACH.**

### REV-F · `audit/findings/manual-findings.md:167-183` SEC-006 fee-recipient wording

- **Source:** REVIEW-4349658229 — same content as CR-3291973189.
- **Verdict:** Same as CR-3291973189. **REJECT-TEACH** (preserves audit pin-trail).

### REV-G · `contracts/facets/MarketDataFacet.sol:42-45` ABI regeneration after IMarketData selector removal

- **Source:** REVIEW-4349628866 (outside-diff)
- **Verdict:** **DEFER-TICKET · Low** (capped at Medium per the rubric's complexity ceiling for cross-repo ABI items; the live PR doesn't actually break the production backend because the removed selectors `getAllPositionIdsByCondition` / `getPositionIdsByMarket` were verified zero-caller across all repos in SCRUM-234). Bundle into a "regenerate `mergedDiamondABI.json` + `scripts/ABI/diamond-abi.json` + verify backend `match-engine/app/utils/settlement_abi.py`" follow-up.
- **Action artefact:** `audit/follow-ups.md` row "CR-ABI-REGEN — regenerate the merged Diamond ABI / `scripts/ABI/diamond-abi.json` post-SCRUM-234 selector removal; confirm `doefin-backend/match-engine/app/utils/settlement_abi.py` has no stale `getAllPositionIdsByCondition` / `getPositionIdsByMarket` references."

### REV-H · `NonceManagerFacet` "legacy orderbook selectors must stay" / "regenerate ABI"

- **Source:** REVIEW-4349628866 outside-diff (24-149)
- **Verdict:** **REJECT-TEACH · Low.** CR is hallucinating "legacy orderbook selectors" — the v2 on-chain orderbook (`OrderCreationFacet`, `OrderManagementFacet`, `LibMatchEngine`) was **fully removed** as part of the v3 cleanup. There ARE no legacy selectors to preserve compatibility with. The backend uses the new `NonceManagerFacet` selectors. CR's "compatibility wrapper" proposal would re-introduce dead surface.
  > Reply: "There are no legacy nonce/cancel selectors to preserve — the v2 on-chain orderbook (`OrderCreationFacet`, `OrderManagementFacet`, `LibMatchEngine`) was removed wholesale in the v3 cleanup. The backend was updated in lock-step to the new selectors. No compatibility shim is appropriate."

### REV-I · `OracleAdapterFacet` blanket `@custom:security`/`@custom:audit`/`@custom:reverts` ask

- **Source:** REVIEW-4349628866 outside-diff
- **Verdict:** **DISCUSS · Low.** Same blanket-policy tension as CR-3291973205/206. This is a view-only facet against an out-of-scope-for-v3 oracle surface — applying the same per-function tag policy as `SettlementFacet` would be expensive and policy-creep. Decision deferred to user. (Likely answer: drop it; the audit corpus already covers the oracle adapter in its own per-domain reviews.)

### REV-J / REV-K / REV-L · MD040 fence-tag asks on `docs/flows/*.md`, `docs/proposal-budget-model-and-1-to-many-matching-v1.2.md`, `.claude/CLAUDE.md:45-51`

- **Source:** REVIEW-4349414578 nitpicks (multiple)
- **Verdict:** **REJECT-TEACH** — folded into the docs-MD040 sweep (recurring-pattern #1).

### REV-M · `audit/scripts/usage-matrix.js` regex escape / grep error handling

- **Source:** REVIEW-4349414578 nitpicks
- **Verdict:** **DEFER-TICKET · Low.** Bundle with CR-USAGE-MATRIX.

### REV-N · `.github/workflows/deadcode-reachability.yml:49-51` `|| echo` masks failures

- **Source:** REVIEW-4349503123 nitpick
- **Verdict:** **DEFER-TICKET · Low.** Folded into the CR-CI-HARDEN ticket already (CR-3291829795 covers the same line).

### REV-O · CR-3291829780 `.coderabbit.yml:525-527` ast-grep schema-validity

- Already covered as CR-3291829780.

### REV-P · `audit/findings-ledger.md:57-59` SEC-005 stale file reference

- Already covered as CR-3291973181.

---

## Follow-up rows to add to `audit/follow-ups.md`

(Adapt headers to that file's schema when applying.)

| ID | Severity | Description |
|---|---|---|
| CR-CI-HARDEN | Low | `.github/workflows/{ci,slither,deadcode-reachability}.yml`: add workflow-level `permissions: { contents: read }`, SHA-pin every `actions/*` + `crytic/slither-action` + `github/codeql-action/upload-sarif`, add `persist-credentials: false` to every `actions/checkout`, drop `pull-requests: write` from deadcode workflow, remove `|| echo "..."` mask from the deadcode step. Bundles CR-3291829786/787/791/795/802, CR-3291829780 (ast-grep schema fix), REV-N. |
| CR-SOLHINT-HARDEN | Low | Promote `.solhint.json` `reason-string` and `gas-custom-errors` from `warn` to `error`; sweep any remaining revert-strings in `contracts/mock/**` and `contracts/audit/**` first. |
| CR-DOCS-MD040 | Low | One-pass `audit/**` + `docs/**` + `.github/CODERABBIT.md` + `.claude/CLAUDE.md` fenced-block language tagging (MD040). Covers CR-3291760041, CR-3291768584 (MD040 only), CR-3291768588, CR-3291829785, CR-3291973176, CR-3291973178, CR-3291973186, REV-J/K/L, plus the existing per-file Quick Win items. |
| CR-DOCS-ABSPATH | Low | Sweep `/Users/reza/...` absolute paths in `audit/**` and `.claude/CLAUDE.md` to repo-relative or sibling-repo paths. Covers CR-3291768582, CR-3291768587, CR-3291973166, CR-3291973191. |
| CR-DOCS-CLEANUP | Low | Misc docs touch-ups: `docs/DEPLOYMENT.md:3` scope tightening (CR-3291760045); `audit/test-engineering/mutation-report.md:442` 585→586 reconciliation (CR-3291973199); `audit/REPORT.md:32` scope-list strike-through (CR-3291973196); `audit/phase2-review/REVIEW.md:11` rewording (CR-3291973195); `.github/CODERABBIT.md:107` KB-upload contradiction (CR-3291973171); `docs/flows/CONDITION_LIFECYCLE.md:812` broken link (CR-3291760046); `audit/fixes/changelog.md:54` `uncheckled` → `unchecked` (CR-3291768585); `audit/findings/dead-code-findings.md:80` A-2 fail-closed guidance (CR-3291973184); `README.md:75`/`299-303` env-file naming (REV-B/C). |
| CR-USAGE-MATRIX | Low | `audit/scripts/usage-matrix.js`: dedupe `uniqueExternalNames` on full signature; classify rows with `-1` counts as UNKNOWN before ORPHAN; escape regex metacharacters in the `names.join("|")` pattern; harden the `e.status === 1` grep catch to distinguish "no matches" from "grep error". Covers CR-3291973197/198 and REV-M. |
| CR-ABI-REGEN | Low | Regenerate `mergedDiamondABI.json`, `mergedDiamondABI.meta.json`, `scripts/ABI/diamond-abi.json` post-SCRUM-234 selector removal; cross-verify `doefin-backend/match-engine/app/utils/settlement_abi.py` and frontend ABI bindings. Covers REV-G. |
| CR-MANIFEST-SHA | Low | Post-merge: backfill `audit/manifest.md:19` "5 — Fix" row with the merged SHA range (CR-3291973193). |
| CR-HARDHAT-VERIFY | Medium | `hardhat.config.js` — fail-fast on missing `ETHERSCAN_API_KEY` for `baseSepolia` / `base` networks (CR-3291760047). |

---

_End of ledger. The 5 ACCEPT-FIX code findings (CR-3291973200 harness solvency, CR-3291973202 setMaxFeeRate, CR-3291973203 zero-fee bypass, CR-3291973204 getPositionInfo, CR-3291973184 dead-code A-2 guidance) plus the 1 ACCEPT-FIX simple doc fix (CR-3291768585 typo) and the 1 mutation-report fix (CR-3291973199) are the items worth applying as a quick follow-up commit on this branch before merge. Everything else is either a DEFER follow-up or REJECT-TEACH._
