# CR triage — PR #24 (SCRUM-236-SCFeeBankPullPayment → v3/dev)

_Generated 2026-05-24T19:52:35Z · commit `2404854498d13d731960e2756ea7fe1dc5977858` · CR comments triaged: 12 (10 inline + 1 review summary outside-diff + 1 walkthrough/release-notes)_

PR title: **SCRUM-236: SC fee bank pull-payment model**. The branch implements the design at `docs/SCRUM-236-fee-bank-design.md` (v2) and was reviewed by `sc-architect`, `sc-business-logic`, and `sc-manual-reviewer` — all three issued `READY-FOR-MERGE` / `READY-FOR-IMPLEMENTATION` verdicts (0 Critical/High/Medium) before CR ran. The PR-branch `.coderabbit.yml` is the OLD revision; the PR-23 teach-yml additions (MD040 sweep, abs-path sweep, CI hardening, view-facet NatSpec) landed on `v3/dev` AFTER this PR's branch was cut, so several recurring patterns re-surface here that would have been auto-suppressed on a rebased branch — noted per-finding below.

CI status at HEAD: **all GREEN** (compile + size-contracts, solhint, slither, hardhat full suite, dead-code reachability, CodeRabbit). The "compile-blocker" CR claim (CR-3295208488) is empirically refuted by the green compile job.

## Summary

| Verdict | Count | Notes |
|---|---|---|
| ACCEPT-FIX | 0 | — |
| REJECT-TEACH | 8 | 1 Info, 7 Low. 4 of these match PR-23.teach.yml patterns (MD040 docs/PR-23-table, NatSpec-style policy already covered, abs-path n/a here, CI-harden n/a here); 3 are deliberate design-doc decisions (FEE_KIND placement, PayoutRedemptionFeePaid event, override-keyword optionality empirically proven by green CI). |
| DEFER-TICKET | 2 | Low (SettlementFacet duplicate `acs` fetch micro-optimisation; SEC-006 test hardening). Bundle into a single follow-up. |
| DISCUSS | 2 | Low. (a) `makerFee` token-arg style consistency on `_settleMerge:766`; (b) PR-23.md verdict-state drift on CR-3291760043 (heading vs body). Both involve a value judgement on tone/policy. |

Severity distribution: **0 Critical, 0 High, 0 Medium, 11 Low, 1 Info**.

**No CR finding overturns the SCRUM-236 reviews' READY-FOR-MERGE verdict.** The single Critical-labelled CR comment (override-missing compile blocker, CR-3295208488) is empirically false — CI compiled cleanly at the same SHA the comment was posted against.

## Recurring false-positive patterns (≥2 occurrences)

All recurring patterns observed on PR-24 are **already covered** by `audit/coderabbit-triage/PR-23.teach.yml`, which has been integrated into `.coderabbit.yml` on `v3/dev` but is NOT yet in this PR's branch. Patterns will be auto-suppressed on subsequent PRs once the PR-24 branch rebases on `v3/dev` (or once v3/dev merges in via standard flow).

| Pattern | Occurrences on PR-24 | Existing PR-23 teach rule | Status |
|---|---|---|---|
| MD040 (untagged code fences in `docs/**` and `audit/**`) | 2 — CR-3295208498 (`docs/SCRUM-236-fee-bank-design.md`), CR-3295208486 (`audit/coderabbit-triage/PR-23.md` table-as-fence proxy) | `docs/**/*.md` and `audit/**/*.md` MD040 single-sweep rule | Covered by PR-23.teach.yml (will auto-suppress post-rebase) |
| Markdown lint heading/table micro-nits on already-merged audit ledgers | 3 — CR-3295208484 (CLAUDE.md MD022), CR-3295208485 (PR-23.md verdict drift), CR-3295208486 (PR-23.md MD056) | The `audit/**` rule explicitly notes the corpus is pinned and per-file comments should be bundled, not raised per-fence | Covered in spirit by PR-23.teach.yml — strengthen on PR-25 if these keep surfacing |

**No new teach-yml file is produced** for PR-24 — the recurring patterns are already in the PR-23 teach.yml that landed on `v3/dev`. The fix is mechanical (rebase) rather than a new YAML rule.

---

## Findings

### CR-3295208488 · REJECT-TEACH · Low

- **Location:** `contracts/facets/AdminConfigFacet.sol:256` (and `:288`)
- **Category:** `protocol-critical`
- **CR's label:** `🔴 Critical` `Quick win` (recorded, not honored)
- **CR's claim:** `withdrawFees` and `getAccruedFees` implement `IAdminConfig` but lack the `override` keyword. Under Solidity 0.8.20 this is a "compile-time error, blocking the fee-bank surface required by `INV-FEE-3` / `INV-FEE-NEW`".
- **Verification:**
  - `IAdminConfig.sol:53` declares `function withdrawFees(address token, uint256 amount) external;` and `:59` declares `function getAccruedFees(address token) external view returns (uint256);`.
  - `AdminConfigFacet.sol:22` declares `contract AdminConfigFacet is IAdminConfig`.
  - `AdminConfigFacet.sol:256` and `:288` lack `override`, while sibling functions `addCollateralToken`, `setMaxFeeRate`, `getMaxFeeRate`, etc. (lines 45, 152, 167, …) all carry `external override`.
  - **However**, the PR's CI job `compile + size-contracts` SUCCEEDED at the exact same head SHA (`2404854…`) the CR comment was posted against — see `gh api /repos/Doefin/doefin-v2/check-runs/77621522412` (conclusion: success). The full hardhat test suite also passed.
  - The Solidity 0.8.20 compiler accepts the file as written. The "compile blocker" claim is empirically false.
- **Grounding:** SCRUM-236 manual review §A.2-A.9 walks `withdrawFees` body line-by-line, citing the live line numbers 256–278; the same reviewer also confirmed CI green. INV-FEE-3 and INV-FEE-NEW HOLD per the manual review §D.
- **Verdict rationale:** REJECT — the headline severity is wrong (compile is proven green), so this is at most a Low style/consistency nit (11-of-13 sibling functions carry `override`, these 2 do not). Not a blocker; the design-doc + reviews + green CI all confirm the fee-bank surface is live.
- **Action artefact:**
  > Reply (post to CR thread):
  > "CI compile + size-contracts passed at this exact SHA (`2404854…`) — see workflow run `26370420891`. The Solidity 0.8.20 compiler accepts this file as written; the claim of a compile-time error is empirically refuted. The `override` keyword absence here IS a style inconsistency with the 11 sibling functions in the same file that DO carry `override`, and we'll fix it as a Low-priority consistency sweep in the post-merge polish commit (tracked alongside CR-DOCS-CLEANUP). It is not a blocker for SCRUM-236; INV-FEE-3 / INV-FEE-NEW are enforced and proved holding by the manual review §D."
  >
  > Teach durably? Optional — could add a path-instruction on `contracts/facets/AdminConfigFacet.sol` reminding CR to verify a "compile blocker" claim against the latest CI run before labelling it Critical. Not raised on this triage because (a) it is the only such occurrence, (b) the green-CI cross-check is a generic CR-triage SOP rather than a CR-specific rule.

---

### CR-3295208493 · DEFER-TICKET · Low

- **Location:** `contracts/facets/SettlementFacet.sol:560` and `:568` (inside `_settleComplementary`)
- **Category:** `protocol-other`
- **CR's label:** `🔵 Trivial` `Low value`
- **CR's claim:** `_settleComplementary` fetches `LibAdminConfigStorage.adminConfigStorage()` twice (once inside each `if (fee > 0)` block) when both `buyerFee` and `sellerFee` are non-zero. `_settleMint` (`:639`) and `_settleMerge` (`:760`) hoist the pointer once before both branches. Asymmetric. ~100 gas saving on the both-fees path.
- **Verification:** Read `_settleComplementary:555-573`, `_settleMint:638-652`, `_settleMerge:759-769`. Confirmed: in `_settleComplementary` the `storage acs = …` declaration is INSIDE each `if (fee > 0)` branch (lines 560 and 568); the other two helpers gate on `if (takerFee > 0 || makerFee > 0)` and declare `acs` once. CR's reading is accurate. The semantic outcome is identical (the `if (fee > 0)` guards make the slot-derivation safe to repeat) — this is purely a micro-optimisation and a consistency nit.
- **Grounding:** No invariant violation. INV-FEE-NEW is satisfied identically by both code shapes; INV-SOLV-3 is unaffected.
- **Verdict rationale:** DEFER — real but trivial. Gas-optimisation findings cap at Medium per `cr-triage` rubric; this is Low. Worth a follow-up consistency commit but not worth a per-PR cycle. The 6th of 6 audit-corpus consistency checks in the manual review §A.8 noted that all 6 fee-pull sites mirror `_settleMint`'s slither-disable rationale — this is the analogous "but the storage hoist is NOT mirrored" companion observation the manual review missed.
- **Action artefact:** Add to `audit/follow-ups.md` (or merge into the existing `CR-DOCS-CLEANUP` row pattern):
  > `CR-SC236-ACS-HOIST` · Low · `contracts/facets/SettlementFacet.sol` — hoist the `LibAdminConfigStorage.AdminConfigStorage storage acs` declaration in `_settleComplementary` once before the `buyerFee/sellerFee` branches, matching the pattern in `_settleMint:639` and `_settleMerge:760`. Saves ~100 gas on the both-fees-positive path; primarily a consistency commit. Bundle with the post-SCRUM-236 polish PR.

---

### CR-3295208495 · DISCUSS · Low

- **Location:** `contracts/facets/SettlementFacet.sol:766-767` (inside `_settleMerge`)
- **Category:** `protocol-other`
- **CR's label:** `🔵 Trivial` `Low value`
- **CR's claim:** `_settleMerge:766` credits `makerFee` to `acs.accruedFees[taker.collateralToken]` and emits with `taker.collateralToken`. While SEC-001 (line 690) proves `taker.collateralToken == maker.collateralToken`, using `maker.collateralToken` here would be semantically clearer and symmetric with `_settleMint:649-650`.
- **Verification:** Confirmed. Line 690: `if (taker.collateralToken != maker.collateralToken) revert Errors.InvalidMatch();` — the tokens are proven equal at every point thereafter. So the SSTORE/event values are byte-identical under either choice. The asymmetry CR points at IS real: in `_settleMint:649-650` the makerFee is keyed and emitted with `maker.collateralToken`; in `_settleMerge:766-767` it is keyed and emitted with `taker.collateralToken`. Not a bug — both options collapse to the same EVM trace.
- **Grounding:** No invariant gain or loss either way. INV-SOLV-2/3, INV-FEE-3, INV-FEE-NEW all hold under either spelling because the equality guard at `:690` reduces the choice to a label.
- **Verdict rationale:** DISCUSS — judgement call on style. Two reasonable positions: (a) prefer `maker.collateralToken` for symmetry with `_settleMint`; (b) prefer `taker.collateralToken` because `_settleMerge`'s taker is the "primary" leg (the entrypoint's `takerOrder`) and the rest of the function uses `taker.*` as the dominant reference (`taker.positionId`, `taker.collateralToken` at line 728 for the burn). Either is defensible. The diff CR proposes is 2 lines; either accepting or rejecting is fine.
- **Action artefact:**
  > Trade-off for the user / sc-developer:
  > - **Option A (accept CR's suggestion):** change `:766-767` to use `maker.collateralToken`. Pro: symmetric with `_settleMint:649-650`. Con: introduces a label inconsistency with `:728` (the CTF burn uses `taker.collateralToken` as the canonical "merge token").
  > - **Option B (keep current code):** add a comment near `:766` saying "tokens are proven equal at `:690`; using `taker.collateralToken` for label consistency with the CTF burn at `:728`." Documents the choice without churn.
  > - **Option C (REJECT outright):** post the reply below.
  >
  > Reply (if rejecting):
  > "Tokens are proven equal at `_settleMerge:690` (InvalidMatch revert), so the spelling is purely cosmetic — the SSTORE and event topic are byte-identical under either choice. The function uses `taker.*` as the canonical leg label elsewhere (`:728` CTF burn uses `taker.collateralToken`); the merge helper is taker-centric while the mint helper is symmetric between taker and maker, hence the asymmetry. No change."

---

### CR-3295208491 · REJECT-TEACH · Low

- **Location:** `contracts/facets/ConditionalTokensFacet.sol:261` (inside `_handlePayoutTransfer`)
- **Category:** `protocol-other`
- **CR's label:** `🔵 Trivial` `Low value`
- **CR's claim:** When `feeBps > 0` but `amount * feeBps < BPS_DENOMINATOR` (extreme dust redemption), `feeAmount` truncates to 0. The `FeeAccrued` emission at `:249` is correctly guarded by `if (feeAmount > 0)` at `:247`, but the legacy `PayoutRedemptionFeePaid` event at `:261` still fires with `feeAmount == 0`. CR itself notes "not an INV-FEE-NEW violation … pre-existing behavior — noting for completeness."
- **Verification:** Confirmed. Read lines 224-262. The short-circuit at `:232-237` only triggers on `feeBps == 0`; in the `feeBps > 0 && feeAmount == 0` truncation case the code proceeds through `:239-261` and emits `PayoutRedemptionFeePaid(recipient, acs.feeReceiver, 0, amount)`. The comment at `:252-256` explicitly says: *"Keep the existing PayoutRedemptionFeePaid event so off-chain redemption analytics still see the per-redeem fee attribution."* — i.e. the unconditional emission is deliberate, to keep the legacy off-chain redemption ETL stable even when no fee accrues.
- **Grounding:** SCRUM-236 manual review §E item 6 explicitly verified the `feeBps == 0` short-circuit ("Lines 232–237 explicitly short-circuit on `feeBps == 0`: just the transfer + return, no `accruedFees` SSTORE, no `FeeAccrued` emission. Fee-zero is a TRUE no-op. CR-3291973203 regression check holds.") but did NOT separately walk the `feeBps > 0 && feeAmount == 0 (truncation)` sub-case. CR's observation is technically novel — but the design-doc deliberately keeps `PayoutRedemptionFeePaid` unconditional for indexer compatibility (architect Q4 and the inline comment).
- **Verdict rationale:** REJECT — the unconditional `PayoutRedemptionFeePaid` emission is a deliberate design choice (comment `:252-256`) to preserve off-chain redemption ETL parity. Off-chain indexers seeing a `(feeAmount=0, userAmount=amount)` event still get the correct payout attribution and can filter on `feeAmount > 0` if they want fee-only events. Adding the guard would BREAK the symmetry promise that "every redemption emits one `PayoutRedemptionFeePaid`." INV-FEE-NEW is unaffected (only governs `FeeAccrued`). At most this is a Style/Info note for the audit corpus, not a defect.
- **Action artefact:**
  > Reply (post to CR thread):
  > "The unconditional `PayoutRedemptionFeePaid` emission is deliberate — see the explicit comment at `:252-256`: keeping the event on every redemption (regardless of whether `feeAmount` truncated to 0) preserves the off-chain redemption-attribution ETL contract. INV-FEE-NEW governs only `FeeAccrued` (correctly guarded at `:247`), and indexers that care about non-zero-fee events filter on `feeAmount > 0`. We considered the guard and chose against it; SCRUM-236 manual review §E item 6 walked the `feeBps == 0` short-circuit and the design-doc architect Q4 chose `LibConstants` placement specifically to keep indexer ETL backward-compatible. No change."
  >
  > Teach durably? No — single occurrence; the deliberate-event-stability framing doesn't generalise to a YAML rule.

---

### CR-3295208497 · REJECT-TEACH · Low

- **Location:** `contracts/libraries/LibConstants.sol:28` and `:32`
- **Category:** `protocol-other`
- **CR's label:** `🟠 Major` `Quick win`
- **CR's claim:** `FEE_KIND_TRADING` and `FEE_KIND_RESOLUTION` (lines 28 and 32) are domain-specific fee constants and "break the SCRUM-234 INFO-3 boundary rule." Cites the project's own `.coderabbit.yml` path-instruction: *"`contracts/libraries/LibConstants.sol`: Shared constants. Only `BPS_DENOMINATOR` lives here today. Flag new constants that are domain-specific — those belong with the owning module."*
- **Verification:** Confirmed line numbers and constant placement. The YAML rule cited DOES exist in `.coderabbit.yml:282-286` (PR-24 branch) — CR's policy-citation is accurate. HOWEVER, the design doc `docs/SCRUM-236-fee-bank-design.md:120` explicitly chose this placement: *"Constants live in `LibConstants` (`FEE_KIND_TRADING`, `FEE_KIND_RESOLUTION`) to avoid magic numbers in settlement code."* — and Q1 at `:317` records the architectural decision: *"Constants defined in `LibConstants.FEE_KIND_TRADING` / `LibConstants.FEE_KIND_RESOLUTION` — no magic numbers in settlement code."* Architecture review at `audit/SCRUM-236-architecture-review.md:365-366` (per branch tree) ACCEPTED this placement. The design therefore overrode the generic boundary rule deliberately, with three reviewer sign-offs.
- **Grounding:** Design doc §5 (event table) and §Q1 (open questions resolution) — explicit placement decision. Architecture review §6 — ACCEPTED. The YAML rule is intentionally stale w.r.t. SCRUM-236; the YAML needs a relaxation comment, not the code a move.
- **Verdict rationale:** REJECT — the placement was a deliberate design-doc-blessed decision overriding the generic boundary rule, sign-off by sc-architect and sc-business-logic and sc-manual-reviewer. The constants are tightly coupled to `Events.FeeAccrued.kind` and used by BOTH `SettlementFacet` AND `ConditionalTokensFacet`, so the "owning module" is ambiguous (the event lives in `Events.sol`); `LibConstants` is the least-coupling location. The YAML rule itself should be relaxed to whitelist these two constants.
- **Action artefact:**
  > Reply (post to CR thread):
  > "The placement was a deliberate design decision — see `docs/SCRUM-236-fee-bank-design.md:120` (event table) and `:317` (Q1 resolution): *'Constants defined in `LibConstants.FEE_KIND_TRADING` / `LibConstants.FEE_KIND_RESOLUTION` — no magic numbers in settlement code.'* Sc-architect (`audit/SCRUM-236-architecture-review.md:365-366`), sc-business-logic, and sc-manual-reviewer all approved. The constants are used by BOTH `SettlementFacet` and `ConditionalTokensFacet` (and their tags are encoded in `Events.FeeAccrued.kind`), so the 'owning module' is ambiguous and `LibConstants` is the least-coupling location. The `.coderabbit.yml:282-286` rule is generic and will be updated in a follow-up to whitelist the two `FEE_KIND_*` constants. No code change."
  >
  > Teach durably? Yes (suggested but not landing as a new YAML in this PR — see below). Add a tightening to the `LibConstants.sol` path-instruction on `v3/dev`'s `.coderabbit.yml`:
  > ```yaml
  > # In .coderabbit.yml at the existing contracts/libraries/LibConstants.sol entry:
  > - path: "contracts/libraries/LibConstants.sol"
  >   instructions: |
  >     Shared constants. `BPS_DENOMINATOR` and the `FEE_KIND_*` discriminators
  >     (TRADING / RESOLUTION, SCRUM-236) live here intentionally — they are
  >     used by both `SettlementFacet` and `ConditionalTokensFacet` and are
  >     encoded into `Events.FeeAccrued.kind`. Flag NEW constants that are
  >     specific to a single facet — those belong with the owning module (see
  >     SCRUM-234 INFO-3 reasoning).
  > ```
  > Folded as a one-line YAML tightening, not a new teach file — the rule is already in the right path-instruction, just needs the FEE_KIND exception. This is the only LibConstants-domain comment on PR-24 so a per-PR teach.yml is not warranted.

---

### CR-3295208498 · REJECT-TEACH · Low

- **Location:** `docs/SCRUM-236-fee-bank-design.md:43` and `:326-357`
- **Category:** `docs-md`
- **CR's label:** `🟡 Minor` `Quick win`
- **CR's claim:** Two untagged fenced code blocks (MD040). Add `text` language tag.
- **Verification:** Confirmed. Line 43 starts a CURRENT/NEW diagram fence with no language tag; the second untagged fence at `:322` is the workstream-plan ASCII art.
- **Grounding:** Recurring pattern #1 in `audit/coderabbit-triage/PR-23.teach.yml`: *"Markdown lint MD040 (fenced code blocks must have a language tag) is accepted policy but executed as a SINGLE sweep ticket (CR-DOCS-MD040), not per-finding."* The teach rule is already in `v3/dev`'s `.coderabbit.yml` but NOT in PR-24's branch (`.coderabbit.yml` on PR-24 is the pre-PR-23 version). After PR-24 rebases on `v3/dev`, future CR runs will not raise this.
- **Verdict rationale:** REJECT-TEACH — recurring pattern explicitly covered by the project's accepted single-sweep policy. Track as part of CR-DOCS-MD040.
- **Action artefact:**
  > Reply (post to CR thread):
  > "Tracked under the `CR-DOCS-MD040` single-sweep follow-up (see `audit/coderabbit-triage/PR-23.md` follow-up matrix). The `docs/**` and `audit/**` MD040 policy is bundled as one ticket — would be auto-suppressed on this PR if rebased on `v3/dev` (which already carries the `audit/coderabbit-triage/PR-23.teach.yml` additions to `.coderabbit.yml`). No per-fence change on this PR."
  >
  > Teach durably? Already covered by PR-23.teach.yml — no new rule needed.

---

### CR-3295208484 · REJECT-TEACH · Low

- **Location:** `.claude/CLAUDE.md:70` (the `### Fee Model …` heading)
- **Category:** `docs-md`
- **CR's label:** `🟡 Minor` `Quick win`
- **CR's claim:** "Add a blank line BEFORE the fee-model heading to satisfy MD022."
- **Verification:** Read `.claude/CLAUDE.md:65-75`. Line 69 IS blank; line 70 is the heading; line 71 is the first bullet immediately after the heading (no blank). CR's claim is FACTUALLY MISDIRECTED — there IS a blank line before the heading (`:69`); the MD022 violation (if any) is the missing blank line AFTER the heading (`:70` → `:71` with no `:71`-blank). Reading `markdownlint`'s MD022 docs: it requires blanks BOTH above and below, but most renderers tolerate the no-blank-after-heading case when the next line is a list (the list itself parses as a block). CR's specific fix instruction is for the wrong side.
- **Grounding:** Same recurring MD022/MD040 corpus-pinned-doc pattern handled by PR-23 teach.yml (the `audit/**/*.md` and `docs/**/*.md` MD040 single-sweep blocks generalise the policy to `.claude/CLAUDE.md` by spirit — the corpus is "pinned" and per-line markdownlint nits are batched, not raised per-PR).
- **Verdict rationale:** REJECT — CR's described problem ("blank line BEFORE") is on the wrong side; the actual `before` line IS already blank (`:69`). Even if we treat the comment generously as "MD022 violation, regardless of side," it's a corpus-doc style nit on a non-render-broken page. The `.claude/CLAUDE.md` file is consumed by Claude Code, not rendered as markdown by a public site, so the render-collapse concern is null.
- **Action artefact:**
  > Reply (post to CR thread):
  > "Line 69 IS already blank — the heading at `:70` already has a blank line preceding it. If you meant MD022's blank-line-AFTER requirement (`:70 → :71` is heading→list-item with no blank between), that's a render-tolerant case for list-following-heading and the `.claude/CLAUDE.md` file is read by Claude Code, not rendered as public markdown — no render-collapse risk. Tracked alongside CR-DOCS-CLEANUP if we do a future spell-and-style sweep. No change on this PR."
  >
  > Teach durably? Already covered by the PR-23.teach.yml docs-corpus-pinned policy (in spirit) — no new rule needed.

---

### CR-3295208485 · DISCUSS · Low

- **Location:** `audit/coderabbit-triage/PR-23.md:49` (heading) vs `:55` and `:59` (body)
- **Category:** `docs-md`
- **CR's label:** `🟡 Minor` `Quick win`
- **CR's claim:** Heading reads `### CR-3291760043 · ACCEPT-FIX · Low` but body says "REJECT — the facet exists" and the trailing line says "(Flipping to REJECT-TEACH; the summary table now reads 5 ACCEPT-FIX.)" — verdict-state drift.
- **Verification:** Confirmed — read PR-23.md `:49-65`. The heading reads `ACCEPT-FIX`, the body explicitly rejects and flips to `REJECT-TEACH`. The summary table at the top of PR-23.md must also be checked for the corresponding count, but the parenthetical at `:59` says "the summary table now reads 5 ACCEPT-FIX" — implying the table count was already updated for the flip. So the drift is contained to the heading vs body inconsistency for this single entry.
- **Grounding:** No invariant impact. PR-23.md is a merged audit triage ledger; CR is asking to fix internal consistency.
- **Verdict rationale:** DISCUSS — judgement call. Two options:
  - **Option A (accept):** Edit PR-23.md `:49` from `ACCEPT-FIX` to `REJECT-TEACH` to remove the drift. Tiny doc churn on an already-merged ledger.
  - **Option B (reject):** PR-23.md is a snapshot artifact pinned to the PR-23 merge SHA; the convention (per `audit/coderabbit-triage/PR-23.teach.yml`'s "pinned to specific commits … APPEND a resolution footnote, not edit the original assertion") is to append a footnote, not edit. Add a one-line footnote at `:65` saying "Note: heading should read REJECT-TEACH per the body; superseded by this footnote."
  - **Option C (reject as out-of-scope):** PR-24 should not edit PR-23's ledger. Track as a follow-up.
- **Action artefact:**
  > Trade-off:
  > - The "audit corpus is pinned" convention (PR-23.teach.yml) argues for Option B (footnote) or Option C (defer).
  > - The "fix the drift" convention argues for Option A.
  > - User's call. Recommendation: Option C (defer to a follow-up housekeeping PR on `audit/coderabbit-triage/` — bundle with any other PR-23 ledger fixes that surface). The drift is recorded here and won't compound.
  >
  > Reply (if Option C):
  > "Acknowledged drift between the heading (`ACCEPT-FIX`) and the body (`REJECT-TEACH`) on CR-3291760043 in PR-23.md. Per the `audit/coderabbit-triage/PR-23.teach.yml` convention this corpus is pinned to its merge SHA; the fix should land as an appended footnote on a future housekeeping PR, not as an in-PR edit. Tracked. No change on this PR."

---

### CR-3295208486 · REJECT-TEACH · Low

- **Location:** `audit/coderabbit-triage/PR-23.md:713-721` (follow-up matrix table)
- **Category:** `docs-md`
- **CR's label:** `🟡 Minor` `Quick win`
- **CR's claim:** Two table rows (MD056) have more than 3 cells because the `Description` column contains literal `|` characters (e.g., `| permissions: { contents: read }`). Normalize to exactly 3 columns.
- **Verification:** Confirmed by reading `:711-721`. Rows like `| CR-CI-HARDEN | Low | ... drop pull-requests: write ... | echo "..."` contain literal `|` inside the description because the description quotes shell snippets and YAML fragments. MD056 fires correctly. Some renderers will truncate visible content past the 3rd column.
- **Grounding:** Recurring pattern of markdown-lint nits on the pinned audit corpus — handled by PR-23.teach.yml's `audit/**/*.md` general policy.
- **Verdict rationale:** REJECT-TEACH — pinned corpus, bundle with other PR-23-ledger fixes (see CR-3295208485 above). Same Option B/C trade-off applies.
- **Action artefact:**
  > Reply (post to CR thread):
  > "Pinned audit corpus — see the `audit/**/*.md` policy in `audit/coderabbit-triage/PR-23.teach.yml`: the convention is to APPEND a footnote, not edit the original ledger. Tracked alongside the CR-3295208485 verdict-state drift fix as a single PR-23-ledger housekeeping commit. No change on this PR."
  >
  > Teach durably? Already covered by PR-23.teach.yml — no new rule needed.

---

### CR-3295208499 · REJECT-TEACH · Low

- **Location:** `test/unit/AdminConfigFacet/AdminConfigFacet.WithdrawFees.test.js:101-117`
- **Category:** `tests`
- **CR's label:** `🟡 Minor` `Quick win`
- **CR's claim:** The `"reverts InsufficientAccruedFees(requested, available) when amount > accrued"` test uses a brittle lowercase substring parse of the revert message instead of `revertedWithCustomError(...).withArgs(...)`. Switch to a selector/args-based assertion helper. Cites the project's path-instruction *"`test/unit/**`: Flag: soft assertions on parameterised custom errors"*.
- **Verification:** Confirmed. Lines 101-117 do `try { … } catch (e) { caught = e; }` then `expect(caught.message.toLowerCase()).to.include('insufficientaccruedfees(...)')`. Sibling test at `:96-98` uses `revertedWith("ZeroAmount()")` (string-based, not custom-error-typed either). The comment at `:105-107` says the author deliberately chose substring because hardhat-chai-matchers behaviour on parameterised custom errors with `withArgs` has varied across runner versions. The project's broader test corpus does use `revertedWithCustomError(...).withArgs(...)` in many places (grep `revertedWithCustomError` across `test/`).
- **Grounding:** No invariant impact. The test still asserts the correct exception with the correct two parameters via substring; a regression that emitted a different custom-error name would still fail the substring assertion. The test is robust to the values; the brittleness CR describes is to (a) runner ABI-format changes that lower-case differently, and (b) selector renames. Neither has happened in this codebase recently.
- **Verdict rationale:** REJECT — the author chose this pattern deliberately (see the explicit comment at `:105-107`) and the assertion DOES bind to the error name AND both parameters (just via substring). A blanket REJECT-TEACH; if the codebase later standardises on `revertedWithCustomError(...).withArgs(...)`, the convergence can happen in a single sweep. Severity is Low.
- **Action artefact:**
  > Reply (post to CR thread):
  > "The substring assertion was a deliberate choice — see the inline comment at `:105-107`: hardhat-chai-matchers' `revertedWithCustomError(...).withArgs(...)` behaviour on parameterised custom errors has varied across runner versions, and the substring-on-the-error-signature pattern was chosen as the most version-stable assertion that still binds to BOTH the error name AND the two parameters (`requested`, `available`). The sibling assertion at `:96-98` uses the same `revertedWith(\"ZeroAmount()\")` pattern. If we standardise on `revertedWithCustomError(...).withArgs(...)` later, we'll do it as a single sweep across the audit pentest suite. No change on this PR."
  >
  > Teach durably? Marginal — could add a path-instruction on `test/unit/AdminConfigFacet/**` noting the substring-binding convention is explicitly chosen for parameterised custom errors. Held off because (a) single-occurrence on PR-24, (b) the project may converge on the typed helper later and we don't want a YAML rule blessing the brittle form.

---

### REV-SUMMARY-OUTSIDE-DIFF · DEFER-TICKET · Low

- **Location:** `test/audit/SEC-006-fee-recipient-and-cap.test.js:35-69` (CR raised this in the review summary as an "outside diff range" comment, not as an inline thread)
- **Category:** `tests`
- **CR's label:** `🟡 Minor` `Quick win`
- **CR's claim:** The test asserts `accruedFees` grew by `2*fee` AND `operator.balance` unchanged, but does NOT assert `feeReceiver.balance` unchanged. A regression that BOTH accrued AND transferred to feeReceiver would still pass — weakening the SCRUM-236 proof intent that fees DO NOT transfer to feeReceiver per-trade. Cites the project guideline *"test/audit/**: Each file proves an ATTACK is blocked AND that the matching CONTROL settles."*
- **Verification:** Confirmed. Read `test/audit/SEC-006-fee-recipient-and-cap.test.js:35-69`. The test captures `accruedBefore` and `operatorColBefore`, asserts `accruedFees` Δ = `2*fee`, and asserts `operator.balance` unchanged. It does NOT capture or assert `feeReceiver.balance`. CR's "regression-that-double-credits-would-pass" argument is valid: a hypothetical buggy `_settleComplementary` that BOTH did `accruedFees += fee` AND `safeTransferFrom(buyer, feeReceiver, fee)` would credit feeReceiver while also passing both existing assertions (operator unchanged; accrued grew by `2*fee`).
- **Grounding:** SCRUM-236 manual review §A.1 (the META-CHECK) proves the production code does NOT transfer to feeReceiver per-trade by enumeration of all 5 Diamond-as-`from` ERC20 sites — but the test suite does not encode this as a runtime assertion on the SEC-006 control path. Adding the assertion is defence-in-depth: a future code change that violated the design intent could escape the META-CHECK (which is a review-time artifact) but would be caught by an enriched test.
- **Verdict rationale:** DEFER — real test-coverage hardening, not a defect in shipped code. Worth a follow-up commit; not worth blocking merge. SCRUM-236 manual review §B explicitly verified the parameter drop; the production-code property holds, and the test enrichment closes the regression-detection loop. Low severity because the design-intent property is independently proven by the manual review META-CHECK.
- **Action artefact:** Add to `audit/follow-ups.md`:
  > `CR-SC236-SEC006-FEE-RECEIVER-FLAT` · Low · `test/audit/SEC-006-fee-recipient-and-cap.test.js` — capture `feeReceiver.balance` before each settlement in the test and assert it is unchanged after, in addition to the existing `accruedFees` Δ and `operator` flat-balance assertions. Closes the "regression-that-both-accrues-and-pays-out-still-passes" gap. CR's suggested 5-line patch is correct as written (`const feeReceiverBefore = await collateral.balanceOf(feeReceiver.address); … expect(await collateral.balanceOf(feeReceiver.address)).to.equal(feeReceiverBefore);`). The `feeReceiver` signer is available via `ctx.signers.feeReceiver` per the existing audit fixture.

---

### CR-WALKTHROUGH-INFO · REJECT-TEACH · Info

- **Location:** `gh issue 24` top-level comment (CR auto-generated walkthrough + release notes)
- **Category:** `docs-md` (no actionable code claim)
- **CR's label:** none (informational summary)
- **CR's claim:** Auto-generated walkthrough/release-notes summary describing the PR's high-level changes. No actionable items.
- **Verification:** Read the full body. No defect claims; pure summary of SCRUM-236 work (fee withdrawal mechanism, FeeAccrued/FeesWithdrawn events, doc/test updates).
- **Verdict rationale:** REJECT-TEACH (Info) — informational only, no action required.
- **Action artefact:**
  > No reply needed. Walkthrough comments are auto-generated and not actionable.
  >
  > Teach durably? No.

---

_End of ledger. 12 CR comments triaged: 0 ACCEPT-FIX (no production code change applied via this triage), 8 REJECT-TEACH (1 Info + 7 Low), 2 DEFER-TICKET (Low, both queued to `audit/follow-ups.md`), 2 DISCUSS (Low, value judgements). All recurring patterns covered by the existing `audit/coderabbit-triage/PR-23.teach.yml` once the PR rebases on `v3/dev`. No new teach.yml is produced. **The SCRUM-236 reviews' READY-FOR-MERGE verdict is not overturned.**_
